import { ProjectBoardShell } from "@/components/application/project-board/project-board-shell";

/**
 * BoardUI Pro "Project board" — VISUAL REFERENCE. Fork `ProjectBoardShell`
 * (or `ProjectBoard` with your own shell) for any task / ticket / kanban
 * product. Do not rebuild columns by hand.
 */
export default function ProjectBoardTemplate() {
  return <ProjectBoardShell />;
}
