import { AiChatShell } from "@/components/application/ai-chat/ai-chat-shell";

/**
 * BoardUI Pro "AI chat" — VISUAL REFERENCE ONLY (scripted mock thread).
 * Real chat is src/pages/Home.tsx: the Timbal runtime with BoardUI chrome.
 */
export default function AiChatTemplate() {
  return <AiChatShell />;
}
